# Reasonix APK（proot 环境）缺陷与配置总览

> 生成时间：2026-08-06（由 Reasonix 排查会话汇总）
> 环境：Android 上的 proot Alpine（BusyBox）环境，宿主 app 经 `proot.so -0 -r ...` 启动；`Seccomp: 2`（两层 filter）+ `NoNewPrivs: 1`。
> 本文件覆盖此目录中旧的通用 README 模板，用于记录 APK 初始状态下可能存在的缺陷、已解决/未解决问题及现有新增配置，供后续会话与维护参考。

---

## 一、已解决的问题

### 1. 无法写入 `/sdcard/Download`（写入边界仅限 `/root`）
- **现象**：`write_file` 写入 `/sdcard/Download/README.md` 被拒（"写入被限制在 /root 可写根内"）。
- **根因**：写入边界默认仅工作区 `/root`，`[sandbox] allow_write` 未配置。
- **修复**：
  - 全局 `/root/.reasonix/config.toml`：`[sandbox] allow_write = ["/sdcard/Download"]`。
  - 项目级 `/root/reasonix.toml`：`allow_write = ["/sdcard/Download", "/usr/local/bin"]`（双保险）。
- **注意**：写入边界与 bash 沙箱配置均为**会话启动时加载**，修改后必须完全退出并重启 Reasonix 进程（`/new` 只开新对话，不重新加载配置）才生效。

### 2. bash 工具不可用（`bash not found in $PATH` / 命令被拒绝执行）
- **现象**：bash 工具报 "bash not found in $PATH"，早期会话还出现 "bash 沙箱不可用"。
- **根因（两层）**：
  1. 本 proot 环境**没有 bash 二进制**，只有 busybox ash（此前被误判为沙箱问题）。
  2. `[sandbox] bash = "enforce"` 且无 bubblewrap 时会拒绝执行任何命令。
- **修复（三管齐下）**：
  1. `/root/.reasonix/config.toml` 与 `/root/reasonix.toml`：`[sandbox] bash = "off"`（proot 无 bubblewrap，必须显式关闭）。
  2. `/root/.reasonix/config.toml`：`[tools.shell] prefer = "bash"`、`path = "/bin/sh"`。
  3. `/root/entry.sh` 每次启动自动在 `/usr/local/bin`、`/usr/bin`、`/bin` 三处创建 bash wrapper（`exec /bin/busybox sh "$@"`），并后台执行 `apk add bash busybox-extras` 安装真 bash。
- **结果**：真 bash 5.2.26（`/bin/bash`，865KB）已装好，bash 工具恢复正常。

### 3. 临时 HTTP 文件服务自动启动失败（`Permission denied`）
- **现象**：`/tmp/start-http.log` 显示 `Permission denied`，8080 服务未拉起。
- **根因**：`write_file` 创建的脚本默认 0644 无执行位，`entry.sh` 直接执行 `/root/start-http.sh` 失败。
- **修复**：`entry.sh` 改用 `sh /root/start-http.sh ...` 显式调用（无执行权限也可运行）。

### 4. 定制 busybox 缺 `httpd` applet
- **现象**：`busybox --list` 无 `httpd`，服务起不来。
- **修复**：`apk add busybox-extras` 提供独立 `/usr/sbin/httpd`（→ `/bin/busybox-extras`）；`start-http.sh` 增加"独立 httpd"模式分支 + `nc` 单文件下载兜底模式。

### 5. httpd 能力检测误判
- **现象**：`busybox httpd --help` 在 applet 缺失时输出 `httpd: applet not found`，同样含 "httpd" 字样，`grep` 会误判为可用。
- **修复**：改用 `busybox --list | grep -qx httpd` 精确匹配。

### 6. apk 源 community 仓库 IO ERROR（strace 装不上）
- **现象**：`apk add strace` 超时/IO ERROR，无法用 strace 定位 syscall。
- **处理**：手动从 Alpine 镜像直接下载 strace APK 解包定位；修复 apk 源后重试安装。最终靠自写 C 诊断程序（`socktest*.c`，用 gcc 编译）完成定位。

### 7. 建立 syscall 级诊断手段
- 编写并编译 `/root/socktest*.c`（socket/bind/listen/accept/accept4/udp/poll/epoll/eventfd/timerfd/socketpair/io_uring 逐项探测），最终精确锁定被拦截的 syscall。

---

## 二、未解决的问题

### 1. 【已修复 2026-08-06】`accept`/`accept4` 被 seccomp 拦截（SIGSYS "Bad system call"）
- **现象**：所有**监听型 TCP 服务**（`busybox httpd`、独立 `httpd`、`nc`、`tcpsvd`、`inetd`、`ftpd`、`tftpd`、`telnetd`）启动即被 SIGSYS 杀死；而出站连接（`wget` 下载 APK）正常。
- **已实测正常的 syscall**：`socket` / `bind` / `listen` / `recvfrom`(UDP) / `poll` / `epoll_create1` / `eventfd2` / `timerfd_create` / `socketpair`。
- **唯一被拦**：`accept`（syscall 202）与 `accept4`（syscall 242），`si_syscall=-1`、`si_call_addr` 落在 `loader.so` 内 —— **proot seccomp 加速 filter 的已知兼容性问题**。
- **标准解法（已验证不可行）**：`PROOT_NO_SECCOMP=1` 退回 ptrace-only 模式 → 在 untrusted_app 环境下 proot 静默退出（guest execve 依赖 seccomp+loader 机制，ptrace 模式不可用）。
- **现状**：8080 服务每次自动启动但自检 `FAIL`（`Connection refused`），手机暂时无法通过本机 HTTP 下载 `reasonix-proot.apk`。
- **最终修复**：proot 从 **proot-me 5.4.0** 替换为 **termux 维护的 proot 5.1.107.89**（michalbednarski 的 Android 定制分支），zig 交叉编译为静态 PIE（musl）。该分支对 Android app 环境的 seccomp/ptrace/socket 有专门适配，app 域下 socket/bind/listen/accept 全部恢复正常（`listen-test` 静态测试程序验证：socket=3, bind=0, listen=0, accept=EAGAIN 正常返回）。
  - 编译要点：自编 mini-talloc；glibc `sys/queue.h`+CIRCLEQ；musl 缺失补丁（`struct rlimit64`/`statfs64`/`TEMP_FAILURE_RETRY`/`MSG_COPY`）；`PROOT_UNBUNDLE_LOADER` 占位编译跳过 objcopy；loader 用链接脚本固定 `0x2000000000`；`offset_to_pokedata_workaround = pokedata_workaround - _start`（0x460）；app 内 `loader.so` 同步换为 termux 版 loader。

### 2. apk 引导依赖网络
- `/root/entry.sh` 的后台引导（`apk add bash busybox-extras`）需要网络；无网时真 bash/httpd 装不上，功能退化（bash 工具靠 wrapper、服务靠 nc 兜底）。
- 引导日志：`/tmp/apk-bootstrap.log`（曾出现 `1 error; 13 MiB in 18 packages`，为 ncurses 相关临时错误，bash 5.2.26 仍安装成功）。

### 3. 局域网 IP 探测受限
- `start-http.sh` 用 `ip -4 addr show` 探测局域网 IP，在 proot 下常拿不到，显示占位符 `<设备局域网IP>`；手机访问需自行确认设备 IP。

---

## 三、现有新增配置与文件清单

| 文件 | 作用 / 关键内容 |
|------|----------------|
| `/root/entry.sh` | 启动入口（每次启动执行）：① 三目录创建 bash wrapper；② 后台 `apk add bash busybox-extras` 引导（日志 `/tmp/apk-bootstrap.log`）；③ `sh /root/start-http.sh` 自动拉起 8080 服务（日志 `/tmp/start-http.log`）；④ 进入 reasonix 会话 |
| `/root/start-http.sh` | 临时 HTTP 静态文件/APK 下载服务：模式 1 = busybox httpd、模式 2 = 独立 httpd（`/usr/sbin/httpd`）、模式 3 = nc 单文件下载兜底；幂等（端口占用即跳过）；启动自检写日志；进程随宿主 app 退出而消失 |
| `/root/reasonix.toml` | `[sandbox] bash = "off"`；`allow_write = ["/sdcard/Download", "/usr/local/bin"]` |
| `/root/.reasonix/config.toml` | `[sandbox] bash = "off"`、`allow_write = ["/sdcard/Download"]`、`network = true`；`[tools.shell] prefer = "bash"`、`path = "/bin/sh"` |
| `/root/socktest*.c`（socktest~socktest5） | 诊断工具：逐 syscall 探测，用于定位 seccomp 拦截点（保留备查） |
| `/tmp/start-http.log` | 服务启动/自检日志 |
| `/tmp/apk-bootstrap.log` | apk 引导安装日志 |
| `/sdcard/Download/reasonix-proot.apk`（约 18 MB） | 待通过 HTTP 分发给安装 app 的手机 |

---

## 四、重启后的验证步骤（供后续会话使用）

1. `bash` 工具应可用（真 bash 5.2.26 或 wrapper 兜底）。
2. 读取 `/tmp/apk-bootstrap.log` 确认 bash/httpd 是否装好。
3. `netstat -tln | grep 8080` 确认服务监听；读取 `/tmp/start-http.log` 看自检结果。
4. 若自检仍 `FAIL`（Connection refused）：确认是否仍为 accept/accept4 被 seccomp 拦截 → 需宿主 app 注入 `PROOT_NO_SECCOMP=1` 后重启 proot。

---

*本 README 生成于 `/sdcard/Download/`（即 `/storage/emulated/0/Download/` 的等价路径；本环境 `/storage` 未挂载）。*
