# 缺陷文档 2

## 一、bash 沙箱不可用（【已修复 2026-08-06】）

**问题现象:**
执行任何 bash 命令都会报错并拒绝运行:

```
error: bash sandbox requested but unavailable on this host; refusing to run unconfined.
Install bubblewrap (`bwrap`) or set [sandbox] bash = "off" in config.toml / Settings -> Sandbox
to restore pre-1.16 unconfined shell execution.
```

**影响:**
- 所有 shell 命令(ls、cp、mv 等)都无法执行,只能改用内置工具(ls、move_file、write_file 等)完成文件操作。
- 验证文件是否迁移成功时无法直接用 shell 检查,需借助 ls 工具查看目录内容。

**原因分析:**
- 配置 `[sandbox] bash = "enforce"` 需要操作系统级沙箱(bubblewrap / bwrap)支持;
- 当前主机未安装 bwrap,沙箱无法建立,出于安全策略拒绝在无隔离环境下运行。

**临时规避:**
- 文件移动使用 `move_file` 工具(而非 shell `mv`),移动 /sdcard 目标目录需配置 `allow_write = ["/sdcard"]`,且写权限在启动时加载,运行中改配置不生效。

**建议修复:**
- 安装 bubblewrap(`bwrap`);或将 config.toml 中 `[sandbox] bash` 改为 `"off"`(按需权衡安全性)。

**实际修复(宿主 app 侧):**
- Android proot 环境无 bwrap 且 user namespace 被禁用,bwrap 不可行 → 采用 `[sandbox] bash = "off"`。
- app 启动时由 `MainActivity.ensureSandboxDisabled()` 在 reasonix 首次运行前预置 `~/.config/reasonix/config.toml`(不存在则创建含 `[sandbox] bash = "off"` 的最小配置;已存在则替换 `bash = "..."` 为 `"off"`)。
- `entry.sh` 每次启动兜底 sed:若配置被改回 `"enforce"` 则自动修正。
- 验证:安装后 logcat 显示 `reasonix sandbox bash disabled: .../root/.config/reasonix/config.toml`,环境正常启动(reasonix 进程运行)。

## 二、文件迁移情况

**目标:**
- 新建的 .md 文件默认存放到 `/storage/emulated/0/Download/`(本机映射为 `/sdcard/Download`)。

**实测记录:**

1. **首次迁移(新建文档.md):**
   - 源路径:`/root/新建文档.md`(16 字节)
   - 目标路径:`/sdcard/Download/新建文档.md`
   - 结果:移动成功,内容完整,源文件已不存在。

2. **本文档(缺陷文档2.md):**
   - 直接写入 `/sdcard/Download/缺陷文档2.md`,验证后确认落盘。

**要点:**
- `/sdcard` 已挂载、可读可写(本次会话 `allow_write = ["/sdcard"]` 已生效)。
- 移动/写入文件一律使用 `move_file` / `write_file` 工具,bash 沙箱不可用时不要尝试 shell 命令。
- 若目标路径不可写,则退回按普通工作区路径 `/root` 创建并在回复中说明。
