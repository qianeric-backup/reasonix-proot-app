<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 存储访问（reasonix 可操作手机文件） -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- 后台运行模式保活：前台服务提升进程优先级，adb 打开其他应用后环境继续在后台运行 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14 (API 34)+：前台服务必须声明类型，本服务用途为特殊用例（保活） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13 (API 33)+：通知需运行时权限，未授权时服务照常运行仅通知不可见 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 无 root 设备 native lib 缺失时引导覆盖安装（系统重新解压 lib） -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="RSXM"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <!-- FileProvider：无 root 修复 native 库时向系统安装器分享 APK -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 后台运行模式保活服务（前台服务，生命周期由开关控制，与 Activity 解耦） -->
        <service
            android:name=".BackgroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
        <!-- ADB 无线调试保活服务：滑动关闭应用后连接保持（前台服务防进程回收） -->
        <service
            android:name=".AdbKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
    </application>
</manifest>
